# v17 모바일 자동 시작 버튼 감사

## 재현 원인
- 기존 빠른 자동 버튼은 `click` 이벤트 하나에 의존했습니다.
- 모바일에서 터치 도중 HUD가 갱신되면 utility bar의 버튼 DOM이 교체되어 tap 입력이 사라질 수 있었습니다.
- iPhone 안전영역에서 설정 패널 시작 위치가 utility bar 하단과 일부 겹칠 수 있었습니다.
- 자동 시작 버튼 외에 `자동진행 대기` 버튼이 한 번 더 노출되어 실제 조작 버튼을 혼동할 수 있었습니다.

## 수정
- pointer-up, touch-end, click을 통합 처리했습니다.
- pointer-down 이후 버튼이 교체되어도 document-level pointer-up fallback으로 동작을 완료합니다.
- 520ms 중복 입력 방지로 한 번의 탭이 자동 시작 후 즉시 수동 전환으로 재실행되지 않게 했습니다.
- 설정창이 열린 상태에서 자동 시작을 누르면 설정창을 닫고 자동진행을 시작합니다.
- utility bar를 설정 패널보다 위에 배치하고 안전영역 간격을 확대했습니다.
- 모바일 간소화 UI에서 중복 `자동진행 대기` 버튼을 숨겼습니다.

## 검증
- iPhone형 390×844 터치 뷰포트에서 설정창을 연 상태로 자동 시작: 성공
- 자동 시작 후 버튼 표기 `수동 전환`: 확인
- 다시 탭해 수동 전환: 성공
- pointer-down과 pointer-up 사이 4회 강제 UI 갱신: 자동 시작 성공
- 모바일 Page/Console 오류: 0건
- 데스크톱 자동↔수동 전환: 정상
